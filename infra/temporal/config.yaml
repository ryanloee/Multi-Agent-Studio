# Temporal configuration for local development
# See: https://docs.temporal.io/references/server-options

server:
  tls: null

persistence:
  defaultStore: postgres
  visibilityStore: postgres

dynamicConfigClient:
  filepath: /etc/temporal/config/dynamicconfig/development.yaml
  pollInterval: "10s"
